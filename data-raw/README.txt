Scripts to recreate the data files in this package from master files on AFS.

To render the .R scripts to html or .Rmd, run knitr::spin(script.R, FALSE), and render
the resulting .Rmd file with rmarkdown2 to whatever format (default html).
